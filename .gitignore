/target
/binaries
*.cdx.json
docs/site/
docs/.venv/
docs/__pycache__/
/core

# Supply-chain pipeline scratch output (ADR-0006) — assembled in CI, never
# committed. Curated statements live in .vex/*.json and ARE tracked.
/docker-build-metadata.json
/grype.json
/grype*.sarif
/vex.auto-*.json
/vex.openvex.json

# OpenWolf assistant state. Never tracked: memory.md, cerebrum.md, the session
# and token ledgers and the cron state accumulate the maintainer's working
# habits, file-access patterns and learned preferences over time, and sceau is
# a public repository. Matches banlieue / bindy / bindcar.
.wolf/
